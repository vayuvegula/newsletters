# Newsletter Whitelist Configuration
#
# This file defines which newsletters to process and how to process them.
# You can customize extraction config and target databases per newsletter.

newsletters:
  # The Batch - AI Newsletter from DeepLearning.AI
  - name: "The Batch"
    email: "thebatch@deeplearning.ai"
    enabled: true

    # Extraction configuration
    # Options: "default", "executive", "technical", "vp_insights", or path to custom config
    extraction_config: "default"  # Uses config/extraction_config.yaml

    # LLM Provider (optional - defaults to llm.default_provider in credentials.yaml)
    # Options: "anthropic", "openai", "gemini"
    # llm_provider: "anthropic"

    # LLM Model (optional - defaults to provider's default_model)
    # llm_model: "claude-sonnet-4-20250514"

    # Database set to use (defined in credentials.yaml)
    # Options: "default", "executive", "technical", or custom database set name
    database_set: "default"

    # Metadata for tracking
    type: "analysis"
    priority: "high"
    tags: ["AI", "ML", "industry-news"]

  # Example: Executive Summary Newsletter
  # - name: "Executive AI Brief"
  #   email: "brief@example.com"
  #   enabled: true
  #   extraction_config: "executive"  # Uses config/extraction_executive.yaml
  #   database_set: "executive"       # Uses different Notion databases
  #   type: "summary"
  #   priority: "high"
  #   tags: ["executive", "brief"]

  # Example: Technical Deep Dive
  # - name: "AI Research Weekly"
  #   email: "research@example.com"
  #   enabled: true
  #   extraction_config: "technical"  # Uses config/extraction_technical.yaml
  #   database_set: "technical"       # Uses research-focused databases
  #   type: "research"
  #   priority: "medium"
  #   tags: ["research", "papers"]

  # Example: VP-Grade Insights (Decision-focused)
  # - name: "The Batch (VP Insights)"
  #   email: "thebatch@deeplearning.ai"
  #   enabled: true
  #   extraction_config: "vp_insights"  # Uses config/extraction_vp_insights.yaml
  #   database_set: "default"
  #   type: "strategic"
  #   priority: "high"
  #   tags: ["VP", "decision-signals", "strategic"]

  # Example: Using Different LLM Providers
  # Use Claude for high-quality analysis
  # - name: "Premium Newsletter"
  #   email: "premium@example.com"
  #   enabled: true
  #   llm_provider: "anthropic"
  #   llm_model: "claude-sonnet-4-20250514"
  #   extraction_config: "default"

  # Use GPT for cost-effective processing
  # - name: "Daily Brief"
  #   email: "daily@example.com"
  #   enabled: true
  #   llm_provider: "openai"
  #   llm_model: "gpt-4o-mini"  # Cheaper model
  #   extraction_config: "default"

  # Use Gemini for balanced cost/quality
  # - name: "Tech News"
  #   email: "tech@example.com"
  #   enabled: true
  #   llm_provider: "gemini"
  #   llm_model: "gemini-2.0-flash-exp"
  #   extraction_config: "default"

  # Add more newsletters here - just copy the template and modify:
  # - name: "Your Newsletter Name"
  #   email: "newsletter@example.com"
  #   enabled: true
  #   extraction_config: "default"    # or "executive", "technical", "vp_insights", or custom path
  #   database_set: "default"         # or your custom database set
  #   type: "analysis"
  #   priority: "medium"
  #   tags: ["tag1", "tag2"]
